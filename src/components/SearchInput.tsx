import React from "react";

const SearchInput = ({}) => (
    <form action="">
        <label htmlFor=""></label>
        <input type="text" name="" id=""/>
    </form>
);

export default SearchInput;
